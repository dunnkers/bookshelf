{
  "name": "bookshelf",
  "version": "1.0.0",
  "author": "dunnkers",
  "dependencies": {
    "handlebars": "^4.7.7",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  },
  "scripts": {
    "build": "rm -rf build && cp -R public build && node compile-website.mjs",
    "serve": "npx -y http-server build",
    "predeploy": "PUBLIC_URL=https://dunnkers.com/bookshelf yarn build",
    "deploy": "gh-pages -d build"
  },
  "homepage": "https://dunnkers.com/bookshelf",
  "license": "MIT"
}
